import { StyleSheet } from "react-native";

const styles = StyleSheet.create({  
  headerLogo: {
    width: 170,
    height: 60,
  },
  headerText: {
    fontSize: 20,
    color: "#ABABAB",

  },
});
  export default styles;